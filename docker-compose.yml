version: '3.8'

services:
  powerup-app:
    build: .
    ports:
      - "3000:80"
    environment:
      - NODE_ENV=development
    restart: unless-stopped
    
  # Optional: Add a database for form submissions
  # postgres:
  #   image: postgres:15-alpine
  #   environment:
  #     POSTGRES_DB: powerup
  #     POSTGRES_USER: admin
  #     POSTGRES_PASSWORD: your_password
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   restart: unless-stopped

# volumes:
#   postgres_data:



